package Polimofirmo;

public interface Shape {
    public double getArea();
}

/*O uso de polimorfismo permite que objetos das classes Circle e Rectangle possam ser tratados como objetos
 da interface Shape, simplificando o código e aumentando a flexibilidade. Por exemplo, uma lista de objetos 
 Shape pode conter tanto objetos Circle quanto objetos Rectangle. */
   
